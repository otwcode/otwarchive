# Read about fixtures at http://ar.rubyonrails.org/classes/Fixtures.html

# Rules for pseuds:
# - every user must have a default pseud
# - every user must have a pseud matching their login
# - only one of a user's pseuds may be default
# 

default_pseud:
  id: 1
  user_id: 1 # basic_user
  name: basic_user
  description: This is a default pseud for basic_user.
  is_default: true

non_default_pseud:
  id: 2
  user_id: 1 # basic_user
  name: Non-Default Pseud
  description: This is a non default pseud for basic_user.
  is_default: false



