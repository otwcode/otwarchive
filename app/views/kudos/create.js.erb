$j("#kudos").replaceWith("<%= escape_javascript(render "kudos/kudos", 
                          { kudos: @kudos, commentable: @commentable, guest_kudos_count: @commentable.guest_kudos_count }) %>");
<% if logged_in? -%>
$j("#kudos_button").remove();
<% end %>
