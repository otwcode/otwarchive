<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01//EN"
"http://www.w3.org/TR/html4/strict.dtd">

<html>
    <head>
        <meta name="generator" content="HTML Tidy, see www.w3.org">
        <meta http-equiv="Content-Type" content=
        "text/html; charset=iso-8859-1">

        <title>
        	An Archive Of Our Own &gt;&gt;
        	<%= controller.action_name %>
		</title>
        <%= stylesheet_link_tag 'archive_core', :media => 'screen' %>
		<%= stylesheet_link_tag 'authentication', :media => 'screen' %>
		<%= stylesheet_link_tag 'click_to_globalize', :media => 'screen' %>
		<%= javascript_include_tag :defaults %>
    </head>

    <body>
        <!-- BEGIN header -->
        <div id="header">
            <div id="login-block">
            <% if logged_in? -%>
	              <ul>
				    <li><span><%= "Hi,".t -%></span> <%= link_to current_user.login, user_path(current_user), :class => 'username' -%>! </li>
				  <!-- Translation button -->
				    <li>
				    	<%= link_to translating_button, :controller => :users, :action => :translation_mode_toggle, :id => @current_user.id -%>
					</li>
				  </ul>	
	              <ul>
	                <li class="first-link"><%= link_to 'Post New'.t, new_work_path -%></li>
	                <li><%= link_to 'Logout'.t, logout_path -%></li>
	              </ul>
            <% elsif logged_in_as_admin? -%>
                <li><span><%= "Hello,".t -%></span> <%= link_to current_admin.login, admin_path(current_admin), :class => 'username username-admin' -%>!</li>
                <ul>
                    <li><%= link_to 'Logout'.t, admin_logout_path -%></li>
                </ul>
			<% else -%>
                <ul>
                    <li><%= link_to 'Login'.t, login_path -%></li>
                </ul>			
			<% end -%>
            </div>

            <h1>An Archive Of Our Own: Extreme Alpha</h1>
			<div class="clearboth"></div>
        </div>
        <!-- END header -->

        <div id="main" class="<%= controller.controller_name + '-' + controller.action_name -%>">
			<%= flash_div :error, :warning, :notice %> 
			<div class="clearboth"></div>
            <%= yield  %>
        </div>
        <!-- END main -->
		
        <!-- BEGIN footer -->
        <div id="footer">
        	<span id="click_to_globalize">
        	<%= @languages_menu -%>	
        	</span>
            <a href="#" title="View License">GPL/CC</a> by the 
			<a href="#" title="The Organization for Transformative Works">OTW</a> &mdash; 
			<a href="#" title="Terms and Conditions">Terms and Conditions</a>
		</div>
        <!-- END footer -->

        <% if ENV["RAILS_ENV"]=="development" %>
		<div id="debug">
			<h1>Development Notice</h1>
			<p>
				This is a development site. That means, please don't put in 
				anything you need to keep secure or want to preserve.
				All the contents of this site (including user accounts) can and 
				probably will be wiped without notice as we work.  
			</p>
			<p>
				Please <strong>do</strong> help us out and 
				<a href="http://code.google.com/p/otwarchive/issues/list">report any problems</a>!
				
			</p>
			<p>
				These links below here are just to provide debugging info for your friendly neighborhood coders,
				please ignore. 
			</p>
			<dl class="debug_navigation">
				<dt><a href="#" onclick="Element.toggle('params_debug_info'); return false;">params</a></dt>
					<dd id="params_debug_info" class="debug_info" style="display:none"><%= debug(params) %></dd>
				<dt><a href="#" onclick="Element.toggle('session_debug_info'); return false;">session</a></dt>
					<dd id="session_debug_info" class="debug_info" style="display:none"><%= debug(session) %></dd>
				<dt><a href="#" onclick="Element.toggle('env_debug_info'); return false;">env</a></dt>
					<dd id="env_debug_info" class="debug_info" style="display:none"><%= debug(request.env) %></dd>
				<dt><a href="#" onclick="Element.toggle('request_debug_info'); return false;">request</a></dt>
					<dd id="request_debug_info" class="debug_info" style="display:none"><%= debug(request) %></dd>
			</dl>
		</div>
        <% end %>

    </body>
</html>

