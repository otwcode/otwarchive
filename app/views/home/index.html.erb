<!-- BEGIN BETA CAVEATS! -->
<noscript>
  <div id="javascript-warning">
    <p>
      <%=h t('js_disabled', :default => "While we've done our best to make this site accessible without javascript, it will work better and look prettier with it enabled. 
      Please consider turning it on!") %>
    </p>
  </div> 
</noscript>
<!-- END BETA CAVEATS! --> 
<div id="signin">  
  <%- if logged_in? || logged_in_as_admin? -%>
    <%= render :partial => 'session/greeting' -%>
  <%- else -%>
    <%= render :partial => 'session/login' -%>
  <%- end -%>
</div>
<div>
  <%= render :partial => 'works/search_box' %>
</div>

<h1><span><%= ArchiveConfig.APP_NAME -%>
  <% unless ArchiveConfig.REVISION.blank? %>(<%=h t('revision', :default => 'rev. {{rev_number}}', :rev_number => ArchiveConfig.REVISION) %>)<% end %></span>
</h1>

<h2><%= t('otw_link', :default => "A project of the {{otw}}", :otw => '<a href="http://transformativeworks.org">Organization For Transformative Works</a>') %></h2>
<hr class="clear" />

<div class="bigwrapper">

<div class="wrapper box">
	<h4>Accounts</h4>
  <h3 id="red"><a href="session/new">Sign in</a></h3>
  <h3 id="red"><a href="">Request an invite</a></h3>
</div>

<div class="wrapper" style="padding-top: 3em;">
	<h3>The Archive of Our Own is a fan-created, fan-run, non-profit, noncommercial archive for transformative fanworks.</h3>
</div>

<div class="wrapper box">
	<h4>Explore</h4>
	<ul class="meters">
		<li><span id="red"><%= link_to t('home_fandoms', :default => 'fandoms'), media_path %></span>: <span><%= @fandom_count -%></span><%= ' ' + t('work_count', :default => " to date") %></li>
		<li><span id="red"><%= link_to t('home_works', :default => 'works'), works_path %></span>: <span><%= @work_count %></span><%= ' ' + t('work_count', :default => " to date") %></li>
    <li><span id="red"><%= link_to t('home_people', :default => 'people'), people_path %></span>: <span><%= @user_count %></span><%= ' ' + t('work_count', :default => " to date") %></li>
	</ul>
</div>

<div class="wrapper lines col1">
  <h3>Updates</h3>
  Incredibly important things that everyone should know: Open Beta is soon!
</div>

<div class="wrapper lines">
  <h3>About</h3>
  If you have questions or want to know more about the Archive check out the <%= link_to 'Archive FAQ', archive_faqs_path %>.
</div>

<div class="wrapper lines">
  <h3>Read</h3>
  <%- if @latest_work -%>
    <%=h t('latest_work', :default => 'Latest work') + ': '%> <span><%= link_to @latest_work.title, @latest_work -%></span>
  <%- end -%>
</div>

</div>

<!--<h5 id="signup"></h5>-->
