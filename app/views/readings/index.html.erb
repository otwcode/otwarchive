<h1>History</h1>
<% if logged_in? && !current_user.readings.empty? %>
  <ul>
    <%= render :partial => 'reading', :collection => @readings %>
  </ul>
<% end %>

<div class="pagination">
	  <%= will_paginate @readings, :prev_label =>  "&laquo; Previous".t,:next_label => "Next &raquo;".t %>
</div>
