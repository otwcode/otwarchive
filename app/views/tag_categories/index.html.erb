<h2>TagWrangler: Category wrangling</h2>
<table>
  <tr>
    <th>Name</th>
    <th>Required</th>
    <th>Exclusive</th>
    <th>Work menu</th>
  </tr>
  <tr>
    <td>Unsorted</td>
    <td></td>
    <td></td>
    <td></td>
    <td></td>
    <td></td>
    <td><%= link_to 'show tags', tag_category_path(0) %></td>
  </tr>
  <% @tag_categories.each do |tag_category| %>
  <tr>
    <td><%= tag_category.display_name %></td>
    <td><% if tag_category.required %>X<% end %></td>
    <td><% if tag_category.exclusive %>X<% end %></td>
    <td><% if tag_category.official %>X<% end %></td>
    <td><%= link_to 'edit', edit_tag_category_path(tag_category) %></td>
    <td>
        <%= link_to "delete", tag_category, :confirm => "This will move all its tags into the unsorted category, are you sure?", :method => :delete %><% if tag_category == TagCategory.default || tag_category == TagCategory.ambiguous -%>*<% end -%>
    </td>
    <td><%= link_to 'show tags', tag_category_path(tag_category) %></td>
  </tr>
  <% end %>
</table>

<p>*Note: If deleted, this category will be re-generated (without any tags) when you refresh the page

<%= tag_wrangler_footer %>
