<h1><%=h t('new_bookmark', :default => 'New bookmark') %></h1>

<%= error_messages_for :bookmark %>

<%= render :partial => 'bookmark_form' %>

<div class="navigation">
<span class="navitem"><%= link_to t('links.back', :default => 'Back'), bookmarks_path %></span>
</div>
