$j('#recent_<%= "#{@bookmark.bookmarkable_type.underscore}_#{@bookmark.bookmarkable_id}"%>').slideUp();
$j('#recent_<%= "#{@bookmark.bookmarkable_type.underscore}_#{@bookmark.bookmarkable_id}"%>').html("");
$j('#recent_link_<%= "#{@bookmark.bookmarkable_type.underscore}_#{@bookmark.bookmarkable_id}"%>').html('<%= escape_javascript(link_to("View most recent bookmarks", {:controller => "bookmarks", :action => "fetch_recent", :id => @bookmark.id, :params => {:fetch => true} }, :method => :get, :remote => true)) %>');
