<% @work = @creation.respond_to?(:work) ? @creation.work : @creation %>
<% if @creation.is_a?(Chapter) %>
  <%= @creation.work.pseuds.map {|p| text_pseud(p) }.to_sentence %> posted a new chapter of 
  "<%= @work.title %>" (<%= work_url(@work) %>) at 
  <%= work_chapter_url(@work, @creation) %>:
<% elsif @creation.is_a?(Work) %><%= @creation.pseuds.map {|p| text_pseud(p) }.to_sentence %> posted a <%= @work.backdate ? "backdated" : "new" %> work:<% end %>

<%= to_plain_text(raw simple_format(get_embed_link(@work))) %>

You're receiving this email because you've subscribed to "<%= @subscription.name %>" (<%= polymorphic_url(@subscription.subscribable) %>). Follow the link to unsubscribe if you no longer wish to receive updates.
