<h4>
<%= @comment.comment_owner ? (link_to @comment.comment_owner_name, @comment.comment_owner) : @comment.comment_owner_name %>
 <%= t('.user_mailer.comment_top', :default => 'left the following comment on') %> 
<%= link_to @commentable.commentable_name, @commentable %>:
</h4>

<p><%=sanitize_limit_and_format_for_display @comment.content %></p>

<%= render :partial => 'comment_notification_footer' %>
