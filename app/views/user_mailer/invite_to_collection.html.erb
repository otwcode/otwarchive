<% content_for :message do %>
  <p>Dear <%= style_bold(@user.default_pseud.byline) %>,</p>

  <p>The collection maintainers of <%= style_link(@collection.title, collection_url(@collection)) %> would
  would like to include your work (<%= style_bold(@work.title.html_safe) %>) in their collection!</p>

  <p>To approve or reject this request, please visit your <%= style_link("collection items", user_collection_items_url(@user)) %> page. </p>

  <p>If in the future you would like to automatically approve the inclusion of
    your works into collections on the Archive, you can edit your
    <%= style_link("Preferences", "#{root_url}#{@user.default_pseud.name}/preferences") %>
    and select the following: <%= style_quote("Automatically agree to your work being collected by others in the Archive.") %>
  </p>

  <p>Thanks!</p>
<% end %>