<% content_for :message do %>
Hello, <%= @user.login %>!

A gift story has been posted for you <%= @collection ? "in the #{@collection.title} collection " : "" %>at the Archive of Our Own!

<% if @collection.nil? %>
"<%= @work.title.html_safe %>" (<%= work_url(@work) %>) (<%= @work.word_count %> <%= ts("words") %>)<% else %>
"<%= @work.title.html_safe %>" (<%= collection_work_url(@collection, @work) %>) (<%= @work.word_count %> <%= ts("words") %>)<% end %>

by <%= "#{(@work.anonymous? ? "an anonymous responder" : @work.pseuds.map {|p| text_pseud(p) }.to_sentence)}" %>

<%= ts("Fandom:") %> <%= @work.fandom_string unless @work.fandom_string.blank? %>

<% unless @work.summary.blank? %><%= ts("Summary:") %>
<%= to_plain_text(raw sanitize_field(@work, :summary)) %><% end %>

<% if @collection && !@collection.gift_notification.blank? %><%= @collection.gift_notification %><% end %><% end %>
