<p>You have been listed as a coauthor on the following <%= @creation.class.name.underscore.humanize.downcase %>:</p>

<p><%= link_to(@creation.title.html_safe, @creation.class == Series ? series_url(@creation) : work_url(@creation)) %></p>

<p>
<%= link_to("Edit the #{@creation.class.name.underscore.humanize.downcase}", @creation.class == Series ? edit_series_url(@creation) : edit_work_url(@creation)) %>
 to remove yourself as author if you've been added in error or don't want to be listed as an author.
</p>
