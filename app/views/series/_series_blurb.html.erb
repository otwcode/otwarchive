<li class="work series" id="series_<%=h series.id %>_blurb">
	<ul class="work">
		<li class="heading">
			<ul class="required-tags">
			<li><img src="/images/category-none.png" alt="" title="dummy icons for layout purpose" /></li>
			<li><img src="/images/category-none.png" alt="" title="dummy icons for layout purpose" /></li>
			<li><img src="/images/category-none.png" alt="" title="dummy icons for layout purpose" /></li>
			<li><img src="/images/category-none.png" alt="" title="dummy icons for layout purpose" /></li>
			</ul>
			<h3 title="Series Title">
				<%= link_to sanitize_title_for_display(series.title), series %>
				<%=h 'by'.t %>
				<%= print_pseud_list(series.pseuds) -%>
				<% if series.hidden_by_admin %>
				<%= image_tag("lockred.png", :size => "15x15", :alt => "(Hidden by Admin)".t) %>
				<% end %>
			</h3>
			
		<h4>&nbsp;<!--fandom will go here--></h4>
		<p class="update"><%= time_ago_in_words(series.updated_at) %></p>
		</li>
		<li>
			<dl class="tags">
			<dt><%=h 'Tags'.t + ': ' %></dt>
			<dd>
			<ul>
				<% series.works.collect(&:tags).flatten.compact.uniq.each do |tag| %>
				<li><%= link_to tag.name, tag_works_path(tag) %></li>
				<% end %>
			</ul>
			</dd>
			</dl>
		</li>
		<li class="summary">
			<%= sanitize_and_format_for_display series.summary %>
		</li>
		<li class="stats">
			<dl>
			<dt><%=h 'Words'.t + ': ' %></dt> 
				<dd><%= '%s'/number_with_delimiter(series.works.collect(&:word_count).sum) %></dd>
			<dt><%=h 'Works'.t + ': ' %></dt> 
				<dd><%=h series.serial_works.count %></dd>
			</dl>
		</li>
	</ul>
</li>