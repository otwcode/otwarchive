<h1><%= t('editing_pseud', :default => 'Editing pseud') %></h1>

<div class="navigation">
	<span class="navitem"><%= link_to t('show_link', :default => 'Show'), [@user, @pseud] %></span>
	<span class="navitem"><%= link_to t('index_link', :default => 'Back To Pseuds'), user_pseuds_path(@user) %></span>
</div>

<%= error_messages_for :pseud %>
<%= render :partial => 'pseuds_form', :locals => { :button_text => t('forms.update', :default => "Update") } %>

