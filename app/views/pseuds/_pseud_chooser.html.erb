<% form_for(@creation) do |f| %>
	<!-- TODO this will not actually work -> figure out how to make it work --> 
	<% f.collection_select :creation, :psueds, current_user.pseuds.find(:all), :id, :name %>
	<% f.submit "Change Pseud" %>
<% end %>