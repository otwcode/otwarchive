<!-- descriptions BACK END is this really a collection page? Is it in a Home View? where does this show up? HALP-->
<div id="collection-page" class="collection profile">
  <!-- /descriptions-->
  <!--subnavigation, sorting and actions-->
  <!--/subnav-->
  <!--main content-->
  <h3><%= ts("#{@prompt.class.name} by %{person}", :person => @prompt.anonymous? ? "Anonymous" : @challenge_signup.pseud.byline) %></h3>

  <div class="collection preface">
    <%= render :partial => "challenge_signups/show_prompt", :locals => {:challenge_signup => @challenge_signup, :prompt => @prompt, :display_anon => "false"} %>
  </div>
  <!--/content-->

  <!--subnav-->
    <%= render :partial => "prompt_controls", :locals => {:challenge_signup => @challenge_signup, :prompt => @prompt} %>
  <!--/subnav-->
</div>

