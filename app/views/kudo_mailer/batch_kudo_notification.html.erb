<% @commentables.each do |commentable| %>
  <p>
  <%= @kudos["#{commentable.class.name}_#{commentable.id}"] %> 
  <%= ts("left kudos on") %>
  <%= link_to commentable.commentable_name.html_safe, polymorphic_url(commentable) %>.
  </p>
<% end %>
