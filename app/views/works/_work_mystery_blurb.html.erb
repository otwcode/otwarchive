<ul class="work mystery">
	<li class="heading">
	<h3><%= t('works.mystery', :default => "Mystery Work") %></h3>
	<h4><%= t('works.will_be_revealed', :default => "{{challenge}}",
    :challenge => work.collections.unrevealed.map {|challenge| link_to(challenge.title, collection_path(challenge))}) %></h4>
	</li>
	<li class="summary"><%= t('works.will_be_revealed', :default => "This work is part of an ongoing challenge, {{challenge}}, and will be revealed soon!",
    :challenge => work.collections.unrevealed.map {|challenge| link_to(challenge.title, collection_path(challenge))}) %></li>
</ul>
