<!-- This partial requires local variables 'work' -->
<% if @work.chapters.count > 1 -%>
  <%= render :partial => 'chapters/chapter', :collection => @work.chapters.find(:all, :order => "position") %>
<% else %>
	<%=sanitize_format @work.chapters.first.content %>
<% end %>
