<h1><%=h t('editing', :default => 'Editing comment') %></h1>

<%= error_messages_for :comment %>

<%= render :partial => 'comments/comment_form', :locals => {:comment => @comment, :commentable => @comment.commentable, :button_name => t('forms.update', :default => "Update")}%>

<div class="navigation">
	<span class="navitem"><%= link_to t('links.show', :default => 'Show'), @comment %></span>
	<span class="navitem"><%= link_to t('links.back', :default => 'Back'), @comment.commentable %></span>
</div>
