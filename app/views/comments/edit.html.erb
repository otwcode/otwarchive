<h1><%=h 'Editing comment'.t %></h1>

<%= error_messages_for :comment %>

<%= render :partial => 'comments/comment_form', :locals => {:comment => @comment, :commentable => @comment.commentable, :button_name => 'Update'.t}%>

<ul class="navigation">
	<li><%= link_to 'Show'.t, @comment %></li>
	<li><%= link_to 'Back'.t, @comment.commentable %></li>
</ul>
