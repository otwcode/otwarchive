<h1><%=h 'Editing comment'.t %></h1>

<%= error_messages_for :comment %>

<%= render :partial => @comment, :locals => {:commentable => @comment.commentable, :button_name => 'Update'.t}%>

<%= link_to 'Show'.t, @comment %> |
<%= link_to 'Back'.t, @comment.commentable %>
