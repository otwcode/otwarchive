<h1><%=h 'New comment'.t %></h1>

<%= error_messages_for :comment %>

<%= render :partial => @comment, :locals => {:commentable => @comment.commentable, :button_name => 'Create'.t}%>

<%= link_to 'Back'.t, @commentable %>
