<!-- expects local variable "challenge_signup" -->
<% challenge_signup.offers.each_with_index do |offer, index| %>
  <h3><%= ts("Offer %{num}", :num => index+1) %></h3>
  <%= render :partial => "challenge_signups/show_prompt", :locals => {:prompt => offer, :display_anon => false} %>
<% end %>
