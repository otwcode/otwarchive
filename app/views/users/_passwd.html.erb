<!-- Password with link to strong password info (still needs to be added) -->
	  <dt><%= f.label :password, h("And your password?").t %> <%=h "(Make it a".t %>&nbsp;<a href="http://passwordstrength.net" tabindex="2"><%=h "strong".t %></a>&nbsp;<%=h "one! It needs to be at least 6 characters long.)".t %></dt>	
<dd><%= f.password_field :password, :tabindex => "3" %></dd>

<!-- Confirm password -->
<dt><%= f.label :password_confirmation, h("Enter it again so we're sure we've got it?").t %></dt>
<dd><%= f.password_field :password_confirmation, :tabindex => "4" %></dd>

