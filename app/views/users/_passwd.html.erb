<!-- Password with link to strong password info (still needs to be added) -->
	  <dt><%= f.label :password, h("And your password?").t %> <%=h "(Make it a".t %>&nbsp;<a href="http://passwordstrength.net" ><%=h "strong".t %></a>&nbsp;<%=h "one! It needs to be at least 6 characters long.)".t %></dt>	
<dd><%= f.password_field :password  %></dd>

<!-- Confirm password -->
<dt><%= f.label :password_confirmation, h("Enter it again so we're sure we've got it?").t %></dt>
<dd><%= f.password_field :password_confirmation %></dd>

